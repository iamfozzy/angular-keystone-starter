import searchComponent from './components/github-search/github-search.directive';

let mod = angular.module('app.common', []);

mod.directive('smSearch', searchComponent);

export default mod = mod.name;