doctype html
html(ng-app='app')
    head
        meta(charset="utf-8")
        meta(name="viewport", content="initial-scale=1.0,user-scalable=no,maximum-scale=1,width=device-width")

        title(ng-bind="(title || 'Keystone Angular Starter')")
        link(rel="shortcut icon", href="/favicon.ico", type="image/x-icon")

        block css
        block head
    body
       block content

    block js
        script(src="/app.js")