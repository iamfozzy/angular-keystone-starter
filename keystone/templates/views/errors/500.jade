extends ../../layouts/app

block content
    .container
        h1= "Error (500) - " + locals.errorTitle
        p.lead Sorry, the site has encountered an error.

        pre= locals.err
        pre= locals.errorMessage